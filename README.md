# SkillsUSA2026_1406

Competitor Number: 1406

Programming language used: Python 3.13.5

Notes: Install the required pip libraries with `pip install -r requirements.txt`

How to run: run `launcher.py` with your native python installation (eg. `python launcher.py`)
